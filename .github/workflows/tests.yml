name: Tests

on:
  push:
    branches: [main, master, develop, 2.x]
  pull_request:
    branches: [main, master, develop, 2.x]

jobs:
  unit-feature:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    strategy:
      fail-fast: false
      matrix:
        php: [8.5]
        laravel: [12.*]
        livewire: [4.*]
        stability: [prefer-stable]
        include:
          - laravel: 12.*
            testbench: 10.*

    name: Unit & Feature - PHP ${{ matrix.php }} - Livewire ${{ matrix.livewire }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: dom, curl, libxml, mbstring, zip, pcntl, pdo, sqlite, pdo_sqlite, bcmath, soap, intl, gd, exif, iconv
          coverage: xdebug

      - name: Setup problem matchers
        run: |
          echo "::add-matcher::${{ runner.tool_cache }}/php.json"
          echo "::add-matcher::${{ runner.tool_cache }}/phpunit.json"

      - name: Install dependencies
        run: |
          composer require "laravel/framework:${{ matrix.laravel }}" "orchestra/testbench:${{ matrix.testbench }}" "livewire/livewire:${{ matrix.livewire }}" --no-interaction --no-update
          composer update --${{ matrix.stability }} --prefer-dist --no-interaction

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*

      - name: Build assets
        run: |
          npm ci
          npm run build

      - name: Execute unit and feature tests
        run: ./vendor/bin/testbench package:test --parallel --testsuite=Unit,Feature

  browser:
    runs-on: ubuntu-latest
    needs: unit-feature
    timeout-minutes: 25
    container:
      image: mcr.microsoft.com/playwright:v1.60.0-noble
      options: --user root

    strategy:
      fail-fast: false
      matrix:
        php: [8.5]
        laravel: [12.*]
        livewire: [4.*]
        stability: [prefer-stable]
        include:
          - laravel: 12.*
            testbench: 10.*

    name: Browser Tests - Livewire ${{ matrix.livewire }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: dom, curl, libxml, mbstring, zip, pcntl, pdo, sqlite, pdo_sqlite, bcmath, soap, intl, gd, exif, iconv
          coverage: none

      - name: Install dependencies
        timeout-minutes: 8
        run: |
          composer require "laravel/framework:${{ matrix.laravel }}" "orchestra/testbench:${{ matrix.testbench }}" "livewire/livewire:${{ matrix.livewire }}" --no-interaction --no-update
          composer update --${{ matrix.stability }} --prefer-dist --no-interaction

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Install npm dependencies and build assets
        timeout-minutes: 5
        run: |
          rm -rf node_modules package-lock.json
          npm install --no-audit --no-fund
          npx playwright install chromium
          npm run build

      - name: Execute browser tests
        timeout-minutes: 15
        run: ./vendor/bin/testbench package:test --parallel --testsuite=Browser

      - name: Upload browser test artifacts
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: browser-test-artifacts-livewire-${{ matrix.livewire }}
          path: |
            tests/Browser/.screenshots
            storage/logs
          retention-days: 7

  code-style:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    name: Code Style

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.5
          extensions: dom, curl, libxml, mbstring, zip, pcntl, pdo, sqlite, pdo_sqlite
          coverage: none

      - name: Install dependencies
        run: composer install --prefer-dist --no-interaction

      - name: Check code style
        run: vendor/bin/pint --test
